These files are refreshed by `python java_check.py` from `java-examples` or by `make check-java` from the project root.
Each Java example in `../README.md` links to its corresponding sample file.
The runner writes only Gradle standard output from the selected test and redacts password-like values from failures.
